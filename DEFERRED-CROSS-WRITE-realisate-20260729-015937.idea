DEFERRED CROSS-WRITE (realisateur was BUSY all session): record the 2026-07-29 /ideate decisions in realisateur's own .scheduler/FOCUS.md. Filed via the front door at /cloture, carrying the full payload -- check-project-busy realisateur re-checked at close and STILL reports BUSY (pid 2643575, a foreign interactive session in realisateur's own project dir since 01:03, verified alive via ps and confirmed NOT this session's pid 2701534). Reader: realisateur's next nightly-batch, which picks up and archives .idea drops.

WHY THE FRONT DOOR AND NOT A [batch] ROW: /cloture step 3 routes a deferred cross-write to a [batch] row in THIS repo's .scheduler/FOCUS.md via focus-commit. Here that is degenerate -- the deferred write's TARGET is realisateur and the stub's DESTINATION is also realisateur, the repo that is busy. The front door is the documented safe-against-a-live-run path (staleness-checked commit), and it was already exercised successfully tonight against this same live lock. Flagging the degenerate case itself as worth a line in cloture.md: step 3 assumes target != this repo, and gives no route when a realisateur-hosted session must defer a write to realisateur.

PAYLOAD -- the session record realisateur's FOCUS.md is owed:

2026-07-29 (/ideate, Zach-directed, run from groc-mangr): THE FOURTH UNREGULATED INTERFACE -- human -> system instruction. Zach's framing: "figure out your shape ... your primary difficulty will be interfacing with my human fallibility, moreso than any other agent."

The gap: UNIVERSE.md names Zach twice -- source of all disturbance, scarcest organ. Both are about RATE, neither about ACCURACY. The QUESTIONS.md contract says a `> ` reply is authoritative, full stop. There is admission control for ideas (park-by-default) and NONE for instructions. Ashby form: the already-conceded bad proprioception (UNIVERSE.md's third interface) produces a bad human mental model, which produces a confident instruction, which is executed faithfully. "The human decides" is precisely the doctrine that makes a false-premise instruction unstoppable, because nothing in the loop is authorized to say "the thing you are deciding about is not true."

Three exhibits: (1) 2026-07-27 bibliothecaire -- Zach asked to unpark a project that was never parked, six sensors green; the false premise BECAME the finding, by instinct not mechanism. (2) groc-mangr -- Zach answered "Yes merge. Always merge." about feature/receipt-ocr, already merged four days earlier in 6e74864. (3) The same answer carried a STANDING POLICY inside a ONE-OFF ANSWER SLOT.

Four decisions, Zach's own selections via AskUserQuestion, filed as a contract amendment through scheduler's front door (9b174dd in scheduler): (A) answers are DIRECTION not instruction, actor re-derives from current state; (B) re-probe the premise before acting, false premise splits by REVERSIBILITY using FOCUS.md's existing stop-and-wait bar; (C) extract standing generalizations into FOCUS.md silently; (D) a clean re-probe reports NOTHING, per ecosim's registered finding that fallible monitors are worse than none because their output is spent out of the scarcest organ. Plus: the three-timescales channel rule is ONE-DIRECTIONAL -- unmixing is an OUTBOUND obligation only; Law 1's corollary forbids asking Zach to pre-sort.

DELIBERATELY NOT A NEW DOCTRINE FILE. Zach chose contract-amendment over both "new UNIVERSE.md section" and "new sibling doctrine file" -- Law 3 says a fifth doctrine surface is never free, and the ecosystem's own line is that prose decays and enforcement does not.

Applied same session in groc-mangr (e8da2c5): both answered blocks processed as direction, two standing decisions extracted, OCR-ownership thread tagged (parked) as past the current milestone.

SECOND ENTRY OWED, same session, already filed separately as FAILURE-FILED-...-015522.idea -- the dispatch-freeze failure (crt runaway). Cross-reference the two: the contract amendment regulates agents re-probing premises under human instructions; the freeze failure is the exact MIRROR, a human holding a premise (the ecosystem is frozen) that no agent could probe because it sits in no channel an agent reads at session start. Premises flow BOTH ways across this interface and only one direction now has a rule. They should be shaped as ONE regulator, not two.

OPEN, NOT DECIDED: groc-mangr sits enabled=0 in _paced.conf with the literal reason "no stability milestone declared." Zach declared the bar himself 2026-07-28, so that reason is now FALSE and nothing escalates a stale park. Re-enable + weight is a stated decision, unanswered this session; note the counterfactual final checkbox means unattended work cannot close that milestone regardless of weight.

HONEST LIMIT ON THIS SESSION'S OWN JUDGMENT: every defer/proceed call tonight rested on check-project-busy, which silence-audit flags [home-scoped] -- it reads job state under $HOME only while 2 accounts dispatch. That is the exact shape of ecosim's registered result that co-blind $HOME-scoped sensors agree perfectly about svc-vaporwave and are all wrong. The realisateur BUSY reading was independently corroborated by ps against a live foreign pid; the scheduler "free" reading was NOT, and any svc-vaporwave-side activity would have been invisible to it.

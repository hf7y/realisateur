FOCUS.md formatting compliance sweep needed for chezz and wtul (flagged by scheduler, 2026-07-22). scheduler's own 'scheduler status <project>' now has a 'next up' section that heuristically parses a project's FOCUS.md for a Current-focus/Priority/Backlog heading and its top-level numbered/bulleted items -- it depends on that structure existing. chezz's FOCUS.md is almost entirely prose/HTML-comment design notes with no real bulleted list, so it can't be parsed at all. wtul has no .claude/FOCUS.md whatsoever -- ROADMAP.md is its real scope file today -- so focus/status come up completely empty for it. Action items, since formatting judgment/reformatting is realisateur's job, not scheduler's: (1) write down a canonical FOCUS.md formatting spec (required headings, list style, common pitfalls -- HTML-comment-only content with no real bulleted next-up section; a ROADMAP.md/FOCUS.md split where neither meets the real scope-file expectation) as one source of truth for both new scaffolds and reformat passes; newer scaffolded projects (groc-mangr etc.) already happen to comply -- worth confirming that's real convention, not luck. (2) reformat chezz's FOCUS.md to match, without losing its existing design-note content (HTML comments can stay as commentary alongside real bullets, just not as the ONLY content). (3) decide and build wtul's real FOCUS.md, reconciled with its existing ROADMAP.md -- migrate content over, symlink, or a thin FOCUS.md pointing at ROADMAP.md as source of truth -- your call which shape is right. Blocking-relationship note (scheduler's own principle, 2026-07-22): scheduler needs to know whether chezz/wtul's next scheduled dispatch should be HELD until this reformat lands, or should run regardless against the current human-readable-but-not-scheduler-parseable format -- that's your judgment call too. Please state it explicitly once decided (e.g. a clear 'blocking' note scheduler can read) so a queued scheduler feature (surfacing blocked-on-another-project relationships in the glance/status views) can act on it mechanically once built.

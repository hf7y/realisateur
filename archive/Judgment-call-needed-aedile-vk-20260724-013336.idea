Judgment call needed: aedile + vkv-inventory's svc-vaporwave crontab migration
never actually completed (confirmed 2026-07-24 -- `crontab -l` for svc-vaporwave
came back empty). Both have been disabled in scheduler's own _paced.conf since
2026-07-20 on the assumption the migration was done ("# migrated to
svc-vaporwave's own crontab") -- 4 days of zero unattended dispatch for both,
undetected. Ownership split (see scheduler DESIGN-NOTES.md 2026-07-24
"silently-orphaned finding" for full writeup): scheduler owns detecting this
kind of drift going forward (a sweep check queued in its own FOCUS.md); this
part -- what to actually DO about it -- is realisateur's call: finish the
svc-vaporwave migration for real (install the crontab), or pull aedile +
vkv-inventory back into zach's own _paced.conf rotation now that Max quota
removes the pressure a second account existed to relieve. svc-vaporwave's
.credentials.json does exist and isn't obviously expired (touched 2026-07-21),
so that's not blocking either path. Whichever way you judge it, the actual
crontab/credential/permission changes on the svc-vaporwave account stay a
human (zach) action, not something to execute directly.

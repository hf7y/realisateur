# verb-kind-lint.ratchet -- commands that were already in the verb build when
# the declaration rule landed, and are forgiven until their own project adds
# `# KIND: verb` to its bin/<name>. See bin/verb-kind-lint.sh.
#
# SHRINKS ONLY. `--accept` drops entries whose command has declared itself or
# left the build; it will NOT add one. Growing this file is a hand edit, on
# purpose, so that it is reviewed. A command that is not listed here and
# declares nothing fails on its first night -- which is the whole requirement,
# since the next product to ship down the workchain will be a NEW arrival and
# not one of these.
#
# SEEDED 2026-08-11 from the build current the day the rule landed
# (2026-08-11T031603Z, 33 commands), NOT from the older build the branch was
# written against: a stale seed reads a verb shipped in between as a new
# arrival, and the guard's first live act would have been a false refusal of
# the nightly cut. THAT WAS THE LAST FREE RE-SEED. From here the list only
# shrinks -- a command that appears in a later build and declares nothing IS a
# new arrival. Re-seeding again would sell back the property being bought.
#
# ---------------------------------------------------------------------------
# THE RULE FOR A CONCESSION, AND THE ONE THAT PAID FOR IT
# ---------------------------------------------------------------------------
#
# A row here may be a plain grandfather. It may also be a CONCESSION -- a
# violation listed on purpose so the lint cannot arm a fleet-wide freeze over
# something whose fix lives in another repository. A concession carries a date
# and a reason, which is the difference between one and an omission. The lint
# still prints it as OWED every run. DO NOT add one without both.
#
# `vim-arcade/vim-arcade` was that concession, and it is gone. It did not end
# the way its own paragraph predicted (a `product` channel that still does not
# exist). It declared `# KIND: personal` instead, and cut-verb-build.sh now
# OMITS a personal tool from the build by name rather than refusing over it --
# so section C reads `ok` and the row went stale on its own. Dropped by
# --accept 2026-09-03.
#
# accepted 2026-09-03T03:03:16Z against a build of 17 command(s)
undeclared bibliothecaire/consulte
undeclared bibliothecaire/cueille
undeclared bibliothecaire/fonde
undeclared bibliothecaire/range
undeclared ecosim/sonde
undeclared gardien/fauche
undeclared gardien/garde
undeclared scheduler/dose

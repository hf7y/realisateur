# path-provenance-audit.ratchet -- the bars that were met when accepted.
# Raised by --accept, never lowered by any flag. See bin/path-provenance-audit.sh.
# accepted 2026-08-07T18:41:09-05:00 on a daily account
# tightened 2026-08-12 on mandark, 31 -> 29: retiring the four `scheduler`
# PATH shims and the orphaned scheduler-overnight-dev.sh driver took the
# undeclared count to 29. Held here so the reduction is a floor, not a mood --
# putting a scheduler shim back on mandark's PATH now fails the build.
# Verified 2026-08-12 via `bash bin/path-provenance-audit.sh --strict`
# (29 undeclared, 42/71 declared, 4/8 met, no regression). Written by hand,
# not by --accept, which the harness classifier declined to run; the number is
# the one that run printed.
bound undeclared 29
nodrift
noorphan

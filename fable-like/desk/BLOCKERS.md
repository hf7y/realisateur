# BLOCKERS — human-only queue (EXAMPLE of the taxonomy you already routed to scheduler)

<!-- One vocabulary, used twice: items here are `blocking` / `waiting` / `fyi`,
     the same three-state judgment as active/parked/waiting in FOCUS.md.
     Swept by /ideate (made explicit in scheduler commit 5041986).
     The glance column counts ONLY `blocking` — a waiting-on-hardware note
     should never look like "needs your decision now". -->

## blocking — needs your decision or your hands, now

- **svc-vaporwave crontab** *(since 2026-07-20 — 5 days!)* — 15-minute cross-account
  install; until done, aedile + vkv-inventory dispatch zero. Human-only by policy.
- **wtul deploy key on dexter** — GitHub repo-admin step; keypair + ssh config
  already staged on dexter, `gh` not installed there.

## waiting — external dependency, no action available

- **gardien**: Thunderbolt-2 cable/adapter (ordered).
- **crt**: (example) Pi 3B peripherals.
- **aedile**: `gh` PAT expiry 2027-07-20 (calendar, not urgent).

## fyi — recorded, nothing owed

- dexter NPU (XDNA) whisper.cpp acceleration idea — parked in crt.

## Recently resolved

- ~~chezz/wtul "credential gap"~~ — RETRACTED 2026-07-24; verified with real test
  push, no gap existed. (The epistemic-discipline lesson.)

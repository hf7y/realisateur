# wtul — radio-station ripping tool

Optical-disc ripping with Spinitron + Discogs metadata for WTUL. Real GitHub
remote; ROADMAP.md is the true scope file. **Moved under the single projects
root** — it was the only project living bare in `~/Documents/`.

## Fable notes

- The half-done dexter move (attempted and reverted same day 2026-07-25) is
  recorded as `waiting` in desk/BLOCKERS.md with the exact human step named:
  install `gh` on dexter OR add the deploy key via web UI. A reverted migration
  with an unnamed remaining step becomes a mystery in three weeks.
- ROADMAP.md stays the real backlog; the thin FOCUS.md list points at it. The
  fable rule: the thin list must name its source of truth in a comment, so
  nobody "fixes" the duplication by deleting the wrong one.
- Discogs chosen for metadata (token in hand); catalog spreadsheet, printer
  model, and web-photo-OCR remain open forks — listed, not silently pending.
